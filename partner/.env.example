# Base URL of the ACP API the partner portal calls (/partner/* routes).
VITE_API_URL=http://localhost:3000
# The merchant admin console — partners deep-link into it to manage a client store.
VITE_ADMIN_URL=http://localhost:5173
