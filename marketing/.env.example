# Base URL of the ACP API the marketing site calls (public /leads/* routes).
VITE_API_URL=http://localhost:3000
# Where "continue setup" sends a prospect after the builder bar (merchant admin signup).
VITE_ADMIN_URL=http://localhost:5173
# Where partners continue (partner portal).
VITE_PARTNER_URL=http://localhost:5176
