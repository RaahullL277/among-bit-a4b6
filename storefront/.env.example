# Base URL of the ACP API the storefront calls (public /storefront routes).
VITE_API_URL=http://localhost:3000
# The store this storefront serves. Defaults to the seeded store (.acp-seed.json).
VITE_STORE_ID=
